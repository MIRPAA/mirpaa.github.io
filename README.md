# Mirpaa Website

This will host a static website for the clinic, under /site

install with uv

    $ uv sync

generate like so

    $ uv run python generate.py

or just

    $ make

commit, push, merge!
